# Задачи 6.1-6.3: Чат

## Обзор

Реализация текстового чата с сохранением сообщений в БД и реальной доставкой через WebSocket.

## 🎯 Цель

- Модель `Message` в Prisma
- Socket.io события для чата
- React UI с историей сообщений
- Сообщения сохраняются и доставляются в real-time

## 📝 Шаг 1: Prisma схема

```prisma
// prisma/schema.prisma
model Message {
  id        String   @id @default(cuid())
  content   String
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  roomId    String
  room      Room     @relation(fields: [roomId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@index([roomId])
  @@index([userId])
}

// Добавить в User и Room
model User {
  messages Message[]
}

model Room {
  messages Message[]
}
```

Миграция: `pnpm prisma migrate dev --name add-messages`

## 📝 Шаг 2: Server (Socket.io)

```typescript
// В WebrtcGateway
@SubscribeMessage('send_message')
async handleMessage(client: Socket, payload: {
  roomId: string;
  content: string;
  userId: string;
}) {
  const message = await this.prisma.message.create({
    data: {
      content: payload.content,
      userId: payload.userId,
      roomId: payload.roomId,
    },
    include: {
      user: {
        select: {
          id: true,
          username: true,
        },
      },
    },
  });

  // Отправить всем в комнате
  this.server.to(payload.roomId).emit('new_message', message);

  return message;
}

@SubscribeMessage('get_history')
async handleGetHistory(client: Socket, payload: { roomId: string }) {
  const messages = await this.prisma.message.findMany({
    where: { roomId: payload.roomId },
    include: {
      user: {
        select: {
          id: true,
          username: true,
        },
      },
    },
    orderBy: { createdAt: 'asc' },
    take: 50,
  });

  return messages;
}
```

## 📝 Шаг 3: Клиент (React)

```typescript
// apps/client/src/hooks/use-chat.ts
export function useChat(roomId: string) {
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState('');

  useEffect(() => {
    // Загрузить историю
    socket.emit('get_history', { roomId }, (messages: Message[]) => {
      setMessages(messages);
    });

    // Новые сообщения
    const handleNewMessage = (message: Message) => {
      setMessages(prev => [...prev, message]);
    };

    socket.on('new_message', handleNewMessage);

    return () => {
      socket.off('new_message', handleNewMessage);
    };
  }, [roomId]);

  const sendMessage = (content: string) => {
    if (!content.trim()) return;

    socket.emit('send_message', {
      roomId,
      content,
      userId: currentUserId,
    });

    setInput('');
  };

  return {
    messages,
    input,
    setInput,
    sendMessage,
  };
}
```

## 📝 Шаг 4: UI компонент

```typescript
// apps/client/src/components/chat.tsx
export function Chat({ roomId, userId }: ChatProps) {
  const { messages, input, setInput, sendMessage } = useChat(roomId);
  const messagesEndRef = useRef<HTMLDivElement>(null);

  // Auto-scroll
  useEffect(() => {
    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages]);

  return (
    <div className="flex flex-col h-full bg-gray-900">
      {/* Сообщения */}
      <div className="flex-1 overflow-y-auto p-4 space-y-2">
        {messages.map(msg => (
          <div
            key={msg.id}
            className={`flex ${msg.userId === userId ? 'justify-end' : 'justify-start'}`}
          >
            <div
              className={`max-w-xs px-4 py-2 rounded-lg ${
                msg.userId === userId
                  ? 'bg-blue-600 text-white'
                  : 'bg-gray-700 text-white'
              }`}
            >
              {msg.userId !== userId && (
                <div className="text-xs text-gray-400 mb-1">
                  {msg.user.username}
                </div>
              )}
              <div>{msg.content}</div>
              <div className="text-xs text-gray-400 mt-1">
                {new Date(msg.createdAt).toLocaleTimeString()}
              </div>
            </div>
          </div>
        ))}
        <div ref={messagesEndRef} />
      </div>

      {/* Input */}
      <div className="p-4 border-t border-gray-700">
        <form
          onSubmit={(e) => {
            e.preventDefault();
            sendMessage(input);
          }}
          className="flex gap-2"
        >
          <input
            type="text"
            value={input}
            onChange={(e) => setInput(e.target.value)}
            placeholder="Написать сообщение..."
            className="flex-1 px-4 py-2 bg-gray-800 text-white rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
          <button
            type="submit"
            className="px-6 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
          >
            Отправить
          </button>
        </form>
      </div>
    </div>
  );
}
```

## ⚠️ Важные нюансы

### 1. XSS защита

```typescript
import DOMPurify from 'dompurify';

const sanitizedContent = DOMPurify.sanitize(content);
```

### 2. Rate limiting

```typescript
// Сервер
const messageCounts = new Map<string, number>();

@SubscribeMessage('send_message')
async handleMessage(client: Socket, payload: { roomId: string; content: string }) {
  const key = `${client.id}:${payload.roomId}`;
  const count = messageCounts.get(key) || 0;

  if (count > 10) {
    throw new WsException('Too many messages');
  }

  messageCounts.set(key, count + 1);

  // Сбросить через минуту
  setTimeout(() => messageCounts.delete(key), 60000);

  // ... создать сообщение
}
```

### 3. Pagination для истории

```typescript
@SubscribeMessage('get_history')
async handleGetHistory(client: Socket, payload: {
  roomId: string;
  page: number;
  limit: number;
}) {
  const skip = (payload.page - 1) * payload.limit;

  const [messages, total] = await Promise.all([
    this.prisma.message.findMany({
      where: { roomId: payload.roomId },
      orderBy: { createdAt: 'desc' },
      skip,
      take: payload.limit,
    }),
    this.prisma.message.count({ where: { roomId: payload.roomId } }),
  ]);

  return {
    messages: messages.reverse(),
    total,
    page: payload.page,
    totalPages: Math.ceil(total / payload.limit),
  };
}
```

## ✅ Критерии выполнения

- [ ] Сообщения сохраняются в БД
- [ ] История загружается при join
- [ ] Новые сообщения приходят в real-time
- [ ] UI отображает сообщения с именами авторов
- [ ] Auto-scroll работает

## 🎮 Следующая: [Фаза 7: SFU](../phase-07-sfu/README.md)

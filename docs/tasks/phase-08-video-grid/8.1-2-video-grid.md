# Задачи 8.1-8.2: Видео-грид

## Обзор

Адаптивный грид для отображения множества видеопотоков. Speaker view для активного говорящего.

## 🎯 Цель

- CSS Grid автоматически перестраивается для N участников
- Каждый video элемент имеет правильный aspect ratio
- Speaker view показывает активного говорящего крупнее
- Audio analysis определяет кто говорит

## 📝 Шаг 1: Адаптивный CSS Grid

```css
/* apps/client/src/components/video-grid.css */
.video-grid {
  display: grid;
  gap: 4px;
  width: 100%;
  height: 100%;
  padding: 4px;
}

/* Динамические колонки через inline style */
.video-grid.cols-1 { grid-template-columns: repeat(1, 1fr); }
.video-grid.cols-2 { grid-template-columns: repeat(2, 1fr); }
.video-grid.cols-3 { grid-template-columns: repeat(2, 1fr); }
.video-grid.cols-4 { grid-template-columns: repeat(2, 1fr); }
.video-grid.cols-5 { grid-template-columns: repeat(3, 1fr); }
.video-grid.cols-6 { grid-template-columns: repeat(3, 1fr); }
.video-grid.cols-7 { grid-template-columns: repeat(4, 1fr); }
.video-grid.cols-8 { grid-template-columns: repeat(4, 1fr); }
.video-grid.cols-9 { grid-template-columns: repeat(3, 1fr); }
.video-grid.cols-10 { grid-template-columns: repeat(4, 1fr); }
.video-grid.cols-11 { grid-template-columns: repeat(4, 1fr); }
.video-grid.cols-12 { grid-template-columns: repeat(4, 1fr); }

.video-grid video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  background: #1a1a1a;
  border-radius: 8px;
}

/* Speaker view - активный говорящий крупнее */
.speaker-grid {
  display: grid;
  grid-template-columns: 1fr;
  grid-template-rows: 1fr auto;
  gap: 4px;
  height: 100%;
}

.speaker-main {
  grid-row: 1;
  height: 70%;
}

.sidebar {
  grid-row: 2;
  height: 30%;
  display: flex;
  gap: 4px;
  overflow-x: auto;
}

.sidebar video {
  min-width: 200px;
  aspect-ratio: 16 / 9;
}
```

## 📝 Шаг 2: React компонент

```typescript
// apps/client/src/components/video-grid.tsx
import { useMemo } from 'react';

interface VideoGridProps {
  children: React.ReactNode[];
  layout?: 'grid' | 'speaker';
  activeSpeaker?: string;
}

export function VideoGrid({ children, layout = 'grid', activeSpeaker }: VideoGridProps) {
  const gridClassName = useMemo(() => {
    const count = children.length;
    if (count <= 2) return 'cols-2';
    if (count <= 4) return 'cols-2';
    if (count <= 6) return 'cols-3';
    return 'cols-4';
  }, [children.length]);

  if (layout === 'speaker' && activeSpeaker) {
    const active = children.find(c => c.key === activeSpeaker);
    const others = children.filter(c => c.key !== activeSpeaker);

    return (
      <div className="speaker-grid">
        <div className="speaker-main">
          {active}
        </div>
        <div className="sidebar">
          {others}
        </div>
      </div>
    );
  }

  return (
    <div className={`video-grid ${gridClassName}`}>
      {children}
    </div>
  );
}
```

## 📝 Шаг 3: Определение активного говорящего

```typescript
// apps/client/src/hooks/use-active-speaker.ts
export function useActiveSpeaker(stream: MediaStream | null) {
  const [isActive, setIsActive] = useState(false);
  const analyserRef = useRef<AnalyserNode | null>(null);

  useEffect(() => {
    if (!stream) return;

    const audioContext = new AudioContext();
    const analyser = audioContext.createAnalyser();
    const source = audioContext.createMediaStreamSource(stream);

    analyser.fftSize = 256;
    analyser.smoothingTimeConstant = 0.8;
    source.connect(analyser);

    analyserRef.current = analyser;

    const dataArray = new Uint8Array(analyser.frequencyBinCount);

    const checkActivity = () => {
      analyser.getByteFrequencyData(dataArray);

      // Средняя амплитуда
      const average = dataArray.reduce((a, b) => a + b) / dataArray.length;

      // Порог активности (настроить экспериментально)
      const isActiveNow = average > 30;

      setIsActive(isActiveNow);

      requestAnimationFrame(checkActivity);
    };

    checkActivity();

    return () => {
      source.disconnect();
      audioContext.close();
    };
  }, [stream]);

  return isActive;
}
```

## 📝 Шаг 4: Hook для всех участников

```typescript
// apps/client/src/hooks/use-speaker-tracking.ts
export function useSpeakerTracking(participants: ParticipantInfo[]) {
  const [activeSpeaker, setActiveSpeaker] = useState<string | null>(null);

  useEffect(() => {
    const activities = new Map<string, boolean>();

    const checkAllSpeakers = () => {
      let maxVolume = 0;
      let newActiveSpeaker: string | null = null;

      for (const [socketId, isActive] of activities) {
        if (isActive) {
          newActiveSpeaker = socketId;
          break; // Первый активный
        }
      }

      if (newActiveSpeaker !== activeSpeaker) {
        setActiveSpeaker(newActiveSpeaker);
      }

      requestAnimationFrame(checkAllSpeakers);
    };

    // Создать AudioContext для каждого потока
    participants.forEach(p => {
      if (p.stream) {
        const audioContext = new AudioContext();
        const analyser = audioContext.createAnalyser();
        const source = audioContext.createMediaStreamSource(p.stream);

        analyser.fftSize = 256;
        source.connect(analyser);

        const dataArray = new Uint8Array(analyser.frequencyBinCount);

        const checkActivity = () => {
          analyser.getByteFrequencyData(dataArray);
          const average = dataArray.reduce((a, b) => a + b) / dataArray.length;

          activities.set(p.socketId, average > 30);

          requestAnimationFrame(checkActivity);
        };

        checkActivity();
      }
    });

    checkAllSpeakers();

    return () => {
      // Cleanup всех AudioContext
    };
  }, [participants, activeSpeaker]);

  return activeSpeaker;
}
```

## ⚠️ Важные нюансы

### 1. AudioContext autoplay policy

```typescript
// AudioContext нужно создавать после пользовательского жеста
const startAudioContext = () => {
  audioContext.resume();
};

document.addEventListener('click', startAudioContext, { once: true });
```

### 2. Memory leaks

```typescript
// Правильный cleanup
useEffect(() => {
  const audioContext = new AudioContext();

  return () => {
    audioContext.close();
  };
}, []);
```

### 3. Производительность AudioContext

Множественные AudioContext могут быть тяжелы. Рассмотри использование одного AudioContext с множественными source nodes.

---

## 📐 Адаптивные сценарии для разного числа участников

### Grid layouts для 2-16+ участников

```typescript
// apps/client/src/hooks/use-grid-layout.ts
export function useGridLayout(participantCount: number) {
  if (participantCount === 1) {
    return { columns: 1, rows: 1, template: '1fr' };
  } else if (participantCount === 2) {
    return { columns: 2, rows: 1, template: 'repeat(2, 1fr)' };
  } else if (participantCount <= 4) {
    return { columns: 2, rows: 2, template: 'repeat(2, 1fr)' };
  } else if (participantCount <= 6) {
    return { columns: 3, rows: 2, template: 'repeat(3, 1fr)' };
  } else if (participantCount <= 9) {
    return { columns: 3, rows: 3, template: 'repeat(3, 1fr)' };
  } else if (participantCount <= 12) {
    return { columns: 4, rows: 3, template: 'repeat(4, 1fr)' };
  } else {
    // 13+ участников — scrollable grid
    return { columns: 4, rows: Math.ceil(participantCount / 4), template: 'repeat(4, 1fr)' };
  }
}
```

### CSS с медиа-запросами

```css
/* apps/client/src/components/video-grid.css */
.video-grid {
  display: grid;
  gap: 8px;
  width: 100%;
  height: 100%;
  padding: 8px;
}

/* 1 участник — full screen */
.video-grid[data-count="1"] {
  grid-template-columns: 1fr;
}

/* 2 участника — side by side */
.video-grid[data-count="2"] {
  grid-template-columns: repeat(2, 1fr);
}

/* 3-4 участника — 2x2 */
.video-grid[data-count="3"],
.video-grid[data-count="4"] {
  grid-template-columns: repeat(2, 1fr);
}

/* 5-6 участников — 3x2 */
.video-grid[data-count="5"],
.video-grid[data-count="6"] {
  grid-template-columns: repeat(3, 1fr);
}

/* 7-9 участников — 3x3 */
.video-grid[data-count="7"],
.video-grid[data-count="8"],
.video-grid[data-count="9"] {
  grid-template-columns: repeat(3, 1fr);
}

/* 10+ участников — 4 колонки с прокруткой */
.video-grid[data-count="10"],
.video-grid[data-count^="1"], /* 10-19 */
.video-grid[data-count^="2"] { /* 20+ */
  grid-template-columns: repeat(4, 1fr);
  overflow-y: auto;
  max-height: 100vh;
}

/* Responsive для мобильных */
@media (max-width: 768px) {
  .video-grid[data-count="3"],
  .video-grid[data-count="4"] {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .video-grid[data-count="5"],
  .video-grid[data-count="6"],
  .video-grid[data-count="7"],
  .video-grid[data-count="8"],
  .video-grid[data-count="9"] {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .video-grid[data-count^="1"],
  .video-grid[data-count^="2"] {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

### React компонент с динамическим data-count

```typescript
// apps/client/src/components/video-grid.tsx
export function VideoGrid({ participants }: { participants: Participant[] }) {
  return (
    <div className="video-grid" data-count={participants.length}>
      {participants.map(p => (
        <VideoTile key={p.id} participant={p} />
      ))}
    </div>
  );
}
```

---

## 📌 Pinned View (закрепление участника)

### Логика закрепления

```typescript
// apps/client/src/hooks/use-pinned-participant.ts
export function usePinnedParticipant() {
  const [pinnedId, setPinnedId] = useState<string | null>(null);

  const togglePin = (participantId: string) => {
    setPinnedId(prev => (prev === participantId ? null : participantId));
  };

  return { pinnedId, togglePin };
}
```

### Layout с закрепленным участником

```css
/* apps/client/src/components/video-grid-pinned.css */
.video-grid-pinned {
  display: grid;
  grid-template-columns: 3fr 1fr; /* Основное видео + sidebar */
  gap: 8px;
  height: 100%;
}

.video-grid-pinned__main {
  /* Закрепленное видео */
  background: black;
}

.video-grid-pinned__sidebar {
  display: grid;
  grid-auto-rows: minmax(120px, 1fr);
  gap: 8px;
  overflow-y: auto;
}
```

### React компонент

```typescript
// apps/client/src/components/video-grid-pinned.tsx
export function VideoGridPinned({
  participants,
  pinnedId,
}: {
  participants: Participant[];
  pinnedId: string | null;
}) {
  const pinnedParticipant = participants.find(p => p.id === pinnedId);
  const otherParticipants = participants.filter(p => p.id !== pinnedId);

  if (!pinnedParticipant) {
    // Fallback к обычному grid
    return <VideoGrid participants={participants} />;
  }

  return (
    <div className="video-grid-pinned">
      <div className="video-grid-pinned__main">
        <VideoTile participant={pinnedParticipant} isPinned />
      </div>
      <div className="video-grid-pinned__sidebar">
        {otherParticipants.map(p => (
          <VideoTile key={p.id} participant={p} />
        ))}
      </div>
    </div>
  );
}
```

### UI для закрепления

```typescript
// apps/client/src/components/video-tile.tsx
export function VideoTile({ participant, onPin }: VideoTileProps) {
  return (
    <div className="video-tile" onDoubleClick={() => onPin?.(participant.id)}>
      <video ref={videoRef} autoPlay playsInline />
      
      <button
        className="pin-button"
        onClick={() => onPin?.(participant.id)}
        title="Pin participant"
      >
        📌
      </button>
    </div>
  );
}
```

---

## 🖥️ Full Screen режим

### Полноэкранный режим для grid

```typescript
// apps/client/src/hooks/use-fullscreen.ts
export function useFullscreen(elementRef: React.RefObject<HTMLElement>) {
  const [isFullscreen, setIsFullscreen] = useState(false);

  const toggleFullscreen = async () => {
    if (!document.fullscreenElement) {
      await elementRef.current?.requestFullscreen();
      setIsFullscreen(true);
    } else {
      await document.exitFullscreen();
      setIsFullscreen(false);
    }
  };

  useEffect(() => {
    const handleFullscreenChange = () => {
      setIsFullscreen(!!document.fullscreenElement);
    };

    document.addEventListener('fullscreenchange', handleFullscreenChange);

    return () => {
      document.removeEventListener('fullscreenchange', handleFullscreenChange);
    };
  }, []);

  return { isFullscreen, toggleFullscreen };
}
```

### Применение в компоненте

```typescript
// apps/client/src/components/room.tsx
export function Room() {
  const gridRef = useRef<HTMLDivElement>(null);
  const { isFullscreen, toggleFullscreen } = useFullscreen(gridRef);

  return (
    <div className="room">
      <div ref={gridRef} className="video-container">
        <VideoGrid participants={participants} />
      </div>

      <div className="controls">
        <button onClick={toggleFullscreen}>
          {isFullscreen ? '⛶ Exit Fullscreen' : '⛶ Fullscreen'}
        </button>
      </div>
    </div>
  );
}
```

### Полноэкранный режим для одного участника

```typescript
// apps/client/src/components/video-tile.tsx
export function VideoTile({ participant }: VideoTileProps) {
  const videoRef = useRef<HTMLVideoElement>(null);
  const { toggleFullscreen } = useFullscreen(videoRef);

  return (
    <div className="video-tile">
      <video ref={videoRef} autoPlay playsInline />
      
      <button
        className="fullscreen-button"
        onClick={toggleFullscreen}
        title="Fullscreen"
      >
        ⛶
      </button>
    </div>
  );
}
```

### CSS для fullscreen

```css
/* apps/client/src/components/video-grid.css */
.video-container:fullscreen {
  background: black;
}

.video-container:fullscreen .video-grid {
  height: 100vh;
  padding: 0;
}

/* Скрыть controls в fullscreen (опционально) */
.video-container:fullscreen + .controls {
  display: none;
}
```

---

## 🎨 Picture-in-Picture (PiP)

### Bonus: Включить PiP для одного видео

```typescript
// apps/client/src/hooks/use-pip.ts
export function usePictureInPicture(videoRef: React.RefObject<HTMLVideoElement>) {
  const [isPiP, setIsPiP] = useState(false);

  const togglePiP = async () => {
    if (!document.pictureInPictureElement) {
      await videoRef.current?.requestPictureInPicture();
      setIsPiP(true);
    } else {
      await document.exitPictureInPicture();
      setIsPiP(false);
    }
  };

  useEffect(() => {
    const handlePiPChange = () => {
      setIsPiP(!!document.pictureInPictureElement);
    };

    videoRef.current?.addEventListener('enterpictureinpicture', handlePiPChange);
    videoRef.current?.addEventListener('leavepictureinpicture', handlePiPChange);

    return () => {
      videoRef.current?.removeEventListener('enterpictureinpicture', handlePiPChange);
      videoRef.current?.removeEventListener('leavepictureinpicture', handlePiPChange);
    };
  }, []);

  return { isPiP, togglePiP };
}
```

---

## ✅ Критерии выполнения

**Базовый grid:**
- [ ] CSS Grid адаптируется для 1-12+ участников
- [ ] Video элементы имеют правильный aspect ratio
- [ ] Speaker view показывает активного говорящего
- [ ] Активный говорящий определяется корректно

**Сценарии для разного числа участников:**
- [ ] 2 участника — side by side
- [ ] 4 участника — 2x2 grid
- [ ] 9 участников — 3x3 grid
- [ ] 16+ участников — scrollable grid

**Pinned view:**
- [ ] Можно закрепить участника (double-click или кнопка)
- [ ] Layout переключается на sidebar режим
- [ ] Остальные участники в sidebar

**Fullscreen:**
- [ ] Кнопка переключает fullscreen для всего grid
- [ ] Можно сделать fullscreen для одного видео
- [ ] ESC выходит из fullscreen

**Опционально:**
- [ ] Picture-in-Picture для одного видео
- [ ] Responsive для мобильных устройств

---

## 🎮 Следующая: [Фаза 9: Деплой и TURN](../phase-09-deploy/9.1-2-deploy-turn.md)

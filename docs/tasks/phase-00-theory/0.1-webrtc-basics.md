# Задача 0.1: Понять основы WebRTC

## Обзор

Изучение фундаментальных концепций WebRTC перед началом разработки. WebRTC — это технология для peer-to-peer коммуникации в браузере, позволяющая передавать аудио, видео и данные без промежуточных серверов.

---

## 🎯 Цель

Понять архитектуру WebRTC и ответить на вопросы:
- Зачем нужен signalling сервер?
- Почему STUN/TURN необходимы?
- Как работает процесс установления соединения?

---

## 📚 Материалы для изучения

### Обязательные к прочтению
1. **[WebRTC для начинающих (habr)](https://habr.com/ru/articles/329466/)**
   - Хорошее введение на русском
   - Описывает базовые понятия

2. **[WebRTC for the Curious](https://webrtcforthecurious.com/)** — главы 1-3
   - Бесплатная книга от создателя WebRTC
   - Глава 1: Что такое WebRTC
   - Глава 2: Архитектура
   - Глава 3: Signalling

3. **[MDN WebRTC API](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)**
   - Официальная документация
   - Примеры кода

### Дополнительные материалы
- [WebRTC signalling flow (MDN)](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API/Signaling_and_video_calling)
- [webrtcglossary.com](https://webrtcglossary.com/) — словарь терминов

---

## 📝 Ключевые концепции

### 1. Signalling (Сигналинг)

**Что это:** Процесс обмена информацией для установления P2P соединения.

**Зачем нужен:** Браузеры не знают друг о друге изначально. Им нужен способ:
- Обнаружить друг друга
- Обменяться информацией о сетевых возможностях
- Согласовать кодеки и форматы

**Важно:** Signalling НЕ передает медиа-данные. Только метаданные для установления соединения.

**Протоколы:** WebSocket, Socket.io, SSE — любой двунаправленный протокол.

---

### 2. STUN (Session Traversal Utilities for NAT)

**Что это:** Сервер, помогающий узнать внешний IP и порт клиента.

**Зачем нужен:** Большинство пользователей за NAT роутерами. Браузер знает только локальный IP (192.168.x.x), а не публичный.

**Как работает:**
```
Клиент → STUN сервер: "Кто я?"
STUN сервер → Клиент: "Ты 203.0.113.5:54321"
```

**Публичные STUN серверы:**
- `stun:stun.l.google.com:19302`
- `stun:stun1.l.google.com:19302`

**Бесплатно:** Google, Twilio и другие предоставляют бесплатный STUN.

---

### 3. TURN (Traversal Using Relays around NAT)

**Что это:** Сервер-посредник для relay трафика.

**Зачем нужен:**
- STUN не всегда работает (symmetric NAT)
- Corporate firewall блокирует P2P
- Некоторые конфигурации NAT не позволяют прямое соединение

**Недостатки:**
- Требует пропускную способность сервера
- Добавляет задержку
- Дорого в эксплуатации

**Когда нужен:** Если нужно работать с любыми NAT конфигурациями.

---

### 4. ICE (Interactive Connectivity Establishment)

**Что это:** Протокол для нахождения лучшего пути соединения.

**Как работает:**
1. Собирает кандидатов (host, srflx, relay)
2. Сортирует по приоритету
3. Пытается соединиться от лучшего к худшему

**Типы кандидатов:**
- `host`: локальный IP
- `srflx`: сервер-рефлексивный (через STUN)
- `relay`: через TURN сервер

---

### 5. RTCPeerConnection

**Что это:** Основной API WebRTC в браузере.

**Основные методы:**
```javascript
const pc = new RTCPeerConnection(configuration);

// Создание offer (для инициатора)
const offer = await pc.createOffer();
await pc.setLocalDescription(offer);

// Создание answer (для принимающей стороны)
const answer = await pc.createAnswer();
await pc.setLocalDescription(answer);

// Установка удалённого описания
await pc.setRemoteDescription(description);

// ICE кандидаты
pc.onicecandidate = (event) => {
  if (event.candidate) {
    // Отправить кандидата через signalling
  }
};

// Получение медиа
pc.ontrack = (event) => {
  // event.streams[0] содержит входящий поток
};
```

---

### 6. SDP (Session Description Protocol)

**Что это:** Текстовый формат описания медиа-сессии.

**Содержит:**
- IP адреса и порты
- Кодеки (opus, VP8, H264)
- Медиа треки (аудио, видео)
- ICE кандидаты

**Пример:**
```
v=0
o=- 123456 2 IN IP4 127.0.0.1
s=-
m=audio 9 UDP/TLS/RTP/SAVPF 111
c=IN IP4 0.0.0.0
a=rtpmap:111 opus/48000/2
```

---

## 🔄 Процесс установления соединения

```
Инициатор                  Принимающая сторона
    |                              |
    |  1. createOffer()            |
    |                              |
    |  2. setLocalDescription()    |
    |                              |
    |  ----- Offer (SDP) --------> |
    |                              | 3. setRemoteDescription()
    |                              | 4. createAnswer()
    |                              | 5. setLocalDescription()
    |                              |
    |  <---- Answer (SDP) ---------|
    |                              |
    |  6. setRemoteDescription()   |
    |                              |
    |  ----- ICE Candidate ------> |
    |                              |
    |  <---- ICE Candidate --------|
    |                              |
    |  (повторяется для всех       |
    |   кандидатов)                |
    |                              |
    |  ===== CONNECTED! ========== |
```

---

## ✅ Критерии выполнения

После изучения ты должен уметь ответить на вопросы:

1. **Почему не могу просто передать видео через WebSocket?**
   - Ответ: WebSocket не оптимизирован для real-time медиа. WebRTC использует UDP, адаптивный bitrate, обработку потерь пакетов.

2. **Зачем нужен signalling, если WebRTC это P2P?**
   - Ответ: Браузерам нужен способ обменяться информацией для установления P2P соединения.

3. **В чём разница между STUN и TURN?**
   - Ответ: STUN помогает узнать публичный IP, TURN релает трафик когда P2P невозможен.

4. **Что такое SDP?**
   - Ответ: Текстовый формат описания медиа-сессии (кодеки, IP, порты).

5. **Что такое ICE кандидат?**
   - Ответ: Возможный путь соединения (IP:порт) с приоритетом.

---

## 🎮 following: Следующая задача

После завершения этой задачи переходи к **[Задача 0.2: Отличия P2P vs SFU](./0.2-p2p-vs-sfu.md)**

---

## 💡 Полезные ресурсы

- [WebRTC Samples](https://webrtc.github.io/samples/) — интерактивные примеры
- [WebRTC for the Curious (полная книга)](https://webrtcforthecurious.com/docs/)
- [JSEP: JavaScript Session Establishment Protocol](https://datatracker.ietf.org/doc/html/draft-ietf-rtcweb-jsep)

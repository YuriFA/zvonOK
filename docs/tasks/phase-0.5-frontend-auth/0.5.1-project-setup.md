# Задача 0.5.1: Настройка React + Vite проекта

> **Статус:** ✅ Выполнено
>
> **Файлы:**
> - `apps/client/vite.config.ts`
> - `apps/client/tsconfig.json`
> - `apps/client/package.json`
> - `apps/client/index.html`
> - `apps/client/src/main.tsx`

## Обзор

Создание современного React приложения с Vite, TypeScript и Tailwind CSS v4 для WebRTC видео-чата.

---

## 🎯 Цель

- React 19 + TypeScript проект
- Vite для разработки и сборки
- Tailwind CSS v4 с design tokens
- ESLint для качества кода
- Горячая замена модулей (HMR)

---

## 📝 Пошаговая инструкция

### Шаг 1: Инициализация проекта

**Создание Vite + React + TypeScript проекта:**

```bash
# В корне monorepo
pnpm create vite apps/client --template react-ts

cd apps/client
pnpm install
```

**Структура проекта:**
```
apps/client/
├── index.html
├── package.json
├── tsconfig.json
├── tsconfig.node.json
├── vite.config.ts
└── src/
    ├── main.tsx
    ├── App.tsx
    ├── App.css
    └── vite-env.d.ts
```

---

### Шаг 2: Настройка Vite

**В `apps/client/vite.config.ts`:**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // Прокси API запросов к бэкенду
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
  },
});
```

**Параметры:**
- `port: 5173` — стандартный порт Vite dev server
- `proxy` — проксирование API запросов к бэкенду (избегает CORS)
- `sourcemap: true` — source maps для отладки

---

### Шаг 3: Настройка TypeScript

**В `apps/client/tsconfig.json`:**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

**Опции:**
- `strict: true` — строгий режим TypeScript
- `noUnusedLocals/Parameters` — ошибки на неиспользуемые переменные
- `paths` — алиас `@/` для импортов

---

### Шаг 4: Настройка Tailwind CSS v4

**Установка:**

```bash
pnpm add -D tailwindcss@next @tailwindcss/vite@next
```

**В `apps/client/vite.config.ts`:**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  // ...
});
```

**В `apps/client/src/index.css`:**

```css
@import "tailwindcss";

@theme {
  --color-primary-50: #f0f9ff;
  --color-primary-500: #0ea5e9;
  --color-primary-600: #0284c7;
  --color-primary-700: #0369a1;

  --color-danger-500: #ef4444;
  --color-danger-600: #dc2626;

  --color-success-500: #22c55e;
  --color-success-600: #16a34a;

  --radius-md: 0.375rem;
  --radius-lg: 0.5rem;

  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
}

@layer base {
  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground;
    font-family: system-ui, -apple-system, sans-serif;
  }
}
```

**Tailwind CSS v4 особенности:**
- `@import "tailwindcss"` вместо `@tailwind` директив
- CSS переменные в `@theme` для design tokens
- `@layer` для базовых стилей

---

### Шаг 5: Настройка ESLint

**В `apps/client/package.json`:**

```json
{
  "scripts": {
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint . --ext ts,tsx --fix"
  }
}
```

**В `apps/client/.eslintrc.cjs`:**

```javascript
module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parser: '@typescript-eslint/parser',
  plugins: ['react-refresh'],
  rules: {
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
  },
};
```

---

### Шаг 6: package.json скрипты

**В `apps/client/package.json`:**

```json
{
  "name": "@webrtc-chat/client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.3",
    "eslint": "^9.13.0",
    "eslint-plugin-react": "^7.37.2",
    "eslint-plugin-react-hooks": "^5.0.0",
    "eslint-plugin-react-refresh": "^0.4.14",
    "globals": "^15.11.0",
    "typescript": "~5.6.2",
    "typescript-eslint": "^8.11.0",
    "vite": "^6.0.1",
    "@tailwindcss/vite": "4.0.0-alpha.26",
    "tailwindcss": "4.0.0-alpha.26"
  }
}
```

---

### Шаг 7: Точка входа

**В `apps/client/src/main.tsx`:**

```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './index.css';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

**В `apps/client/index.html`:**

```html
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WebRTC Chat</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

---

## 🚀 Запуск проекта

```bash
# В apps/client/
pnpm dev
```

Откроется: http://localhost:5173

---

## ✅ Критерии выполнения

- [x] Vite проект создан
- [x] TypeScript настроен
- [x] Tailwind CSS v4 работает
- [x] `pnpm dev` запускает dev server
- [x] HMR (горячая замена) работает
- [x] ESLint настроен

---

## 🎮 Следующая задача

После завершения переходи к **[Задача 0.5.2: UI Компоненты](./0.5.2-ui-components.md)**

---

## 💡 Полезное

### Vite CLI команды

```bash
# Dev server
pnpm dev

# Production build
pnpm build

# Превью production сборки
pnpm preview

# Type check
tsc --noEmit
```

### Переменные окружения

**В `apps/client/.env.local`:**

```env
VITE_API_BASE_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
AGORA_APP_ID=your-agora-app-id
```

**Использование в коде:**

```typescript
const apiUrl = import.meta.env.VITE_API_BASE_URL;
```

> Важно: переменные должны начинаться с `VITE_` для доступности в клиентском коде
